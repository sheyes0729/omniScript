export function add(a: int, b: int): int {
  return a + b;
}

export class Greeter {
  name: string;
  init(name: string) {
    this.name = name;
  }
  greet(): string {
    return "Hello " + this.name;
  }
}
